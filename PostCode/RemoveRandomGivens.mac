
/* Removes random values until no more can be removed whilst preserving a unique solution  */

RemoveRandomGivens(SudokuMatrix):=block(
    SeedRandom(),
    CombMatrix:ConvertSM2CM(SudokuMatrix),
    GenListRemCells,
    GenAllPossValuesList,
    GetRandomCell,
    GetPositions(),
    RemovedValue:,
    SetValue,
    SetValue,
    NotPossibles,
    
);

